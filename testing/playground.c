// Check the data size of unsigned char and float

int main(){
    int size_of_char = sizeof(unsigned char);
    int size_of_float = sizeof(float);

    printf("Size of unsigned char: %d\n", size_of_char);
    printf("Size of float: %d\n", size_of_float);
}